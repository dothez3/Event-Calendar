{% extends "base.html" %}

{% block content %}
<div class="container" style="max-width: 700px;">

  <h2 class="mb-3">Message Detail</h2>

  <div class="card shadow-sm">
    <div class="card-body">

      <h5 class="mb-2">
        From {{ notification.sender.name }}
        {% if notification.recipient_id %}
          · To {{ notification.recipient.name }}
        {% else %}
          · To all employees
        {% endif %}
      </h5>

      {% if notification.project %}
        <p class="mb-2">
          <strong>Project:</strong> {{ notification.project.name }}
        </p>
      {% endif %}

      <p class="small text-muted mb-3">
        Sent on {{ notification.created_at.strftime('%m/%d/%Y %I:%M %p') }}
      </p>

      <hr>

      <p style="white-space: pre-wrap; font-size: 1rem;">
        {{ notification.message }}
      </p>

      <div class="mt-4">
        <a href="{{ url_for('notifications') }}" class="btn btn-secondary">
          Back to Notifications
        </a>
        <form method="post"
              action="{{ url_for('notification_delete', notification_id=notification.id) }}"
              class="d-inline">
            <button class="btn btn-danger"
                    onclick="return confirm('Delete this message?')">Delete</button>
        </form>
      </div>

    </div>
  </div>

</div>
{% endblock %}
